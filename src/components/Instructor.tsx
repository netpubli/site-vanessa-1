export default function Instructor() {
  return (
    <section id="instructor" aria-label="Instructor">
      {/* Instructor — bio, credentials, photo */}
      <div className="py-24 flex items-center justify-center">
        <p className="text-white/30 text-sm">[ Instructor ]</p>
      </div>
    </section>
  )
}
